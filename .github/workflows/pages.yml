name: Publish public site

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/pages.yml"
      - ".nvmrc"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"
      - "packages/shared/**"
      - "packages/dashboard/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    if: github.event.repository.private == false
    name: Publish canonical dashboard
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit

      - name: Build shared packages
        run: npm run build:core

      - name: Build canonical public site
        env:
          VITE_PUBLIC_SITE: "true"
        run: npm run build --workspace @safeguard-sentinel/dashboard -- --base=/SafeGuard-Sentinel/

      - name: Test landing, architecture, and dashboard experience in Chrome
        run: npm run test:public --workspace @safeguard-sentinel/dashboard

      - name: Configure GitHub Pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: packages/dashboard/dist

      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
